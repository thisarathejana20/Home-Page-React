import { Box } from "@mui/material";
import Header from "../components/Header";

const Home = () => {
  return (
    <Box sx={{ overflow: "hidden" }}>
      <Header />
    </Box>
  );
};

export default Home;
